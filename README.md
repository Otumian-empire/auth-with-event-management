# AUTH WITH EVENT MANAGEMENT

- **Runtime:** Node.js (with TypeScript for type safety).
- **Infrastructure:** Redis (Streams & Consumer Groups).

Use PUB-SUB to handle email, etc that may happen after system is set up
